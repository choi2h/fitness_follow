package com.ffs.domain.product.repository;

import com.ffs.domain.product.Product;
import org.springframework.data.repository.Repository;

public interface ProductRepository extends Repository<Product, Long> {
}
